# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Python.gitlab-ci.yml

# Official language image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/python/tags/
image: python:$PYTHON_VERSION  # Change image dynamically depending on Python version.

# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/topics/caching/
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - test

# Define a template for testing with different Python versions
.test_template: &test_template
  stage: test
  before_script:
    - python --version  # For debugging
    - python -m pip install --upgrade pip
    - pip install virtualenv

    # Create and activate the virtual environment
    - virtualenv venv
    - source venv/bin/activate
    - pip install poetry  # Install poetry within the virtual environment
    - poetry --version  # Verify poetry installation
    - ls -R  # List all files to ensure package files are in place
  script:
    - pip install -e .
    - pip install -r ./requirements.txt
    - pytest -v

# Use YAML anchors to create separate jobs for each Python version dynamically
test:python39:
  <<: *test_template
  variables:
    PYTHON_VERSION: "3.9"

test:python310:
  <<: *test_template
  variables:
    PYTHON_VERSION: "3.10"

test:python311:
  <<: *test_template
  variables:
    PYTHON_VERSION: "3.11"

test:python312:
  <<: *test_template
  variables:
    PYTHON_VERSION: "3.12"

test:python313:
  <<: *test_template
  variables:
    PYTHON_VERSION: "3.13"


#  artifacts:
#    paths:
#      - dist/*.whl

# pages:
#   script:
#     - pip install sphinx sphinx-rtd-theme
#     - cd doc
#     - make html
#     - mv build/html/ ../public/
#   artifacts:
#     paths:
#       - public
#   rules:
#     - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

